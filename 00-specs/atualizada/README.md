# 00-specs/atualizada/

Consolidated current state of the specs. **This is what every subagent reads by default.**

- Seeded from `00-specs/originais/` when the session is initialized.
- Updated by the orchestrator when a new `alteracoes/YYYY-MM-DD/` folder is consolidated.
- Do NOT edit files here directly — always go through `alteracoes/`.
